#!/usr/bin/env ruby
## A nice process for committing per-file messages.
# TODO: Remove ruby dep (for impoverished sysems)
$VERBOSE = true
`git status --porcelain`.split("\n").each do |line|
    file = line.split(/\s+/)[-1]
    system 'git', 'commit', '-v', file
end
