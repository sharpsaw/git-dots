#!/bin/bash -xe
## Clone with --depth 1 to get you to the code faster (and `gdeepen` is always available, later on)
. ~/.sh/maybe-hub-alias
hub clone --depth 1 "$@"
: OK. Now, at any time, you can deepen the history with: gdeepen
